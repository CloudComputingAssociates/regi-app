/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Lifecycle state of a Rotation. staged = being built (on deck); active = currently eating through it (at bat)
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "RotationStatus".
 */
export type RotationStatus = "staged" | "active";

/**
 * Rotation model: a Rotation owns a set of Menus via menu entries, each with a planned and consumed count. Users 'affirm' a Menu they ate; consumedCount increments and the rotation drains over time. Also covers shopping list, AI recipe, and food lookup payloads driven from the rotation flow.
 */
export interface RotationSchema {
  [k: string]: unknown;
}
/**
 * A user-owned rotation: a set of Menus the user is currently eating through, sized for a target span and people count.
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "Rotation".
 */
export interface Rotation {
  /**
   * Server-assigned primary key
   */
  id?: number;
  /**
   * Owner of the rotation
   */
  userId?: number;
  /**
   * Display name for the rotation
   */
  name: string;
  /**
   * How many days the rotation covers (2–10)
   */
  spanDays: number;
  /**
   * Number of people the menus are sized for (1–10)
   */
  peopleCount: number;
  /**
   * Day of week the user typically starts (e.g. 'Monday'). Optional — the UX adapts if absent.
   */
  pinDay?: string | null;
  /**
   * Optional calendar anchor date (YYYY-MM-DD). When set, enables day-count display and macro budget balance tracking.
   */
  pinDate?: string | null;
  status: RotationStatus;
  /**
   * User-saved favorite flag
   */
  isFavorite: boolean;
  /**
   * When the rotation was first created
   */
  createdAt?: string;
  /**
   * When the rotation was last modified
   */
  updatedAt?: string;
  [k: string]: unknown;
}
/**
 * Request body for POST /api/rotation — creates an empty rotation skeleton (no menus yet)
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "CreateRotationRequest".
 */
export interface CreateRotationRequest {
  /**
   * Display name for the rotation
   */
  name?: string;
  /**
   * How many days the rotation covers (2–10)
   */
  spanDays: number;
  /**
   * Number of people the menus are sized for (1–10). Defaults to 1.
   */
  peopleCount?: number;
  /**
   * Day of week the user typically starts (e.g. 'Monday'). Optional.
   */
  pinDay?: string | null;
  /**
   * Optional calendar anchor date (YYYY-MM-DD).
   */
  pinDate?: string | null;
  [k: string]: unknown;
}
/**
 * Request body for PATCH /api/rotation/{id} — all fields optional; only provided fields are updated
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "UpdateRotationRequest".
 */
export interface UpdateRotationRequest {
  /**
   * New display name
   */
  name?: string;
  /**
   * New people count (1–10)
   */
  peopleCount?: number;
  /**
   * New favorite flag
   */
  isFavorite?: boolean;
  status?: RotationStatus;
  [k: string]: unknown;
}
/**
 * Which of the user's daily macro targets the day's menu satisfies
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "MacroTargetsHit".
 */
export interface MacroTargetsHit {
  /**
   * True when the day's protein target is hit
   */
  protein: boolean;
  /**
   * True when the day's fiber target is hit
   */
  fiber: boolean;
  /**
   * True when the day's carbs are under the user's ceiling
   */
  carbsUnderCeiling: boolean;
  /**
   * True when the day's fat is above the user's floor
   */
  fatAboveFloor: boolean;
  [k: string]: unknown;
}
/**
 * One menu entry in a Rotation — a Menu with its planned and consumed counts
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "RotationMenuEntry".
 */
export interface RotationMenuEntry {
  /**
   * Server-assigned primary key
   */
  id?: number;
  /**
   * Owning Rotation ID
   */
  rotationId?: number;
  /**
   * Menu attached to this menu entry
   */
  menuId: number;
  /**
   * Denormalized menu name for display
   */
  menuName?: string;
  /**
   * Whether the referenced Menu is Pinned (in the Binder)
   */
  pinned: boolean;
  /**
   * How many times this Menu appears in the rotation
   */
  plannedCount: number;
  /**
   * Incremented when a DailyLog using this Menu is finalized
   */
  consumedCount: number;
  /**
   * Computed: plannedCount - consumedCount
   */
  remainingCount?: number;
  /**
   * Display order in the rotation's menus panel
   */
  sortOrder?: number;
  /**
   * When the menu entry was created
   */
  createdAt?: string;
  [k: string]: unknown;
}
/**
 * GET /api/rotation/{id} response: the rotation header plus its menus
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "RotationDetail".
 */
export interface RotationDetail {
  /**
   * Server-assigned primary key
   */
  id?: number;
  /**
   * Owner of the rotation
   */
  userId?: number;
  /**
   * Display name for the rotation
   */
  name: string;
  /**
   * How many days the rotation covers (2–10)
   */
  spanDays: number;
  /**
   * Number of people the menus are sized for (1–10)
   */
  peopleCount: number;
  /**
   * Day of week the user typically starts (e.g. 'Monday'). Optional.
   */
  pinDay?: string | null;
  /**
   * Optional calendar anchor date (YYYY-MM-DD).
   */
  pinDate?: string | null;
  status: RotationStatus;
  /**
   * User-saved favorite flag
   */
  isFavorite: boolean;
  /**
   * When the rotation was first created
   */
  createdAt?: string;
  /**
   * When the rotation was last modified
   */
  updatedAt?: string;
  /**
   * The Menus in this rotation and their consumption counts
   */
  menus: RotationMenuEntry[];
  [k: string]: unknown;
}
/**
 * Request body for POST /api/rotation/{id}/menus
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "AddMenuEntryRequest".
 */
export interface AddMenuEntryRequest {
  /**
   * Menu to add to the rotation
   */
  menuId: number;
  /**
   * How many times this Menu should appear in the rotation
   */
  plannedCount?: number;
  /**
   * Display order in the rotation's menus panel
   */
  sortOrder?: number;
  [k: string]: unknown;
}
/**
 * Request body for PUT /api/rotation/{id}/menus/{menuId}
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "UpdateMenuEntryRequest".
 */
export interface UpdateMenuEntryRequest {
  /**
   * New planned count for this menu entry
   */
  plannedCount?: number;
  /**
   * New display order in the rotation's menus panel
   */
  sortOrder?: number;
  [k: string]: unknown;
}
/**
 * Aggregate macro totals for a menu or a slot
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "MenuMacros".
 */
export interface MenuMacros {
  /**
   * Total calories
   */
  calories: number;
  /**
   * Total protein in grams
   */
  proteinG: number;
  /**
   * Total carbohydrate in grams
   */
  carbG: number;
  /**
   * Total fat in grams
   */
  fatG: number;
  /**
   * Total fiber in grams
   */
  fiberG: number;
  /**
   * Total sodium in milligrams
   */
  sodiumMg: number;
  [k: string]: unknown;
}
/**
 * One meal stacked in a slot (position 0–3), denormalized for display.
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "MenuSlotMeal".
 */
export interface MenuSlotMeal {
  position: number;
  mealId: number;
  mealName?: string | null;
  mealType?: string | null;
  mealImageThumbnail?: string | null;
  clonedFromMealId?: number | null;
  macros?: MenuMacros;
  [k: string]: unknown;
}
/**
 * One slot in a Menu. Slot order maps 1→A, 2→B, … 10→J via SlotOrderToLabel.
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "MenuSlot".
 */
export interface MenuSlot {
  /**
   * 1-based slot order within the menu (1–10)
   */
  slotOrder: number;
  /**
   * Display letter computed from slotOrder (A–J). Read-only; clients should not send it.
   */
  slotLabel: string;
  /**
   * Optional user-set label for the slot (e.g., 'breakfast', 'lunch'). Null when unnamed.
   */
  slotName?: string | null;
  /**
   * True when the user marked this slot as dining out — the slot is intentionally empty
   */
  isDiningOut: boolean;
  /**
   * Meals stacked in this slot (0–4), ordered by position. Empty when the slot is empty or dining-out.
   */
  meals: MenuSlotMeal[];
  macros?: MenuMacros1;
  [k: string]: unknown;
}
/**
 * Aggregate macro totals for a menu or a slot
 */
export interface MenuMacros1 {
  /**
   * Total calories
   */
  calories: number;
  /**
   * Total protein in grams
   */
  proteinG: number;
  /**
   * Total carbohydrate in grams
   */
  carbG: number;
  /**
   * Total fat in grams
   */
  fatG: number;
  /**
   * Total fiber in grams
   */
  fiberG: number;
  /**
   * Total sodium in milligrams
   */
  sodiumMg: number;
  [k: string]: unknown;
}
/**
 * A reusable named menu — an ordered list of slots, each of which may hold a Meal
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "Menu".
 */
export interface Menu {
  /**
   * Server-assigned primary key
   */
  id?: number;
  /**
   * Owner of the menu
   */
  userId?: number;
  /**
   * Display name for the menu
   */
  name: string;
  /**
   * Number of slots in the menu (1–10)
   */
  slotCount: number;
  /**
   * True when this menu is an admin-curated YEH menu visible to all users
   */
  isYeh: boolean;
  /**
   * User-saved favorite flag
   */
  isFavorite: boolean;
  /**
   * Binder flag — 1 = pinned menu (survives rotation teardown; only explicit delete kills it), 0 = disposable
   */
  pinned: boolean;
  /**
   * Back-pointer set on copies minted by POST /menu/{id}/duplicate to the source menu id; null on originals. Read-only / server-owned.
   */
  clonedFromMenuId?: number | null;
  /**
   * Cached total calories across all slots
   */
  totalCalories?: number | null;
  /**
   * Cached total protein (g) across all slots
   */
  totalProteinG?: number | null;
  /**
   * Cached total fat (g) across all slots
   */
  totalFatG?: number | null;
  /**
   * Cached total carbohydrate (g) across all slots
   */
  totalCarbG?: number | null;
  /**
   * Cached total fiber (g) across all slots
   */
  totalFiberG?: number | null;
  /**
   * Cached total sodium (mg) across all slots
   */
  totalSodiumMg?: number | null;
  /**
   * All slots in the menu, ordered by slotOrder ascending
   */
  slots: MenuSlot[];
  /**
   * When the menu was first created
   */
  createdAt?: string;
  /**
   * When the menu was last modified
   */
  updatedAt?: string;
  [k: string]: unknown;
}
/**
 * 409 body of PUT /api/menu/{id}/save-to-original — the copy can't overwrite its original because one or more slotted meals aren't saved to the binder yet. unsavedMeals lists their names for the user prompt.
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "SaveMenuToOriginalConflict".
 */
export interface SaveMenuToOriginalConflict {
  error: string;
  unsavedMeals: string[];
  [k: string]: unknown;
}
/**
 * Lightweight menu reference. Slots and macros are optional for skeleton listings; note carries a reuse hint when the same menu appears multiple times.
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "MenuSummary".
 */
export interface MenuSummary {
  /**
   * Menu ID
   */
  id: number;
  /**
   * Menu display name
   */
  name: string;
  /**
   * Optional slot list — included on detail responses, omitted on lightweight skeleton responses
   */
  slots?: MenuSlot[];
  menuTotals?: MenuMacros;
  /**
   * Optional reuse hint, e.g. "Reused — same as Day N"
   */
  note?: string;
  [k: string]: unknown;
}
/**
 * Request body for POST /api/menu — creates an empty menu with N empty slots
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "CreateMenuRequest".
 */
export interface CreateMenuRequest {
  /**
   * Display name for the menu (server defaults if omitted)
   */
  name?: string;
  /**
   * Number of slots to create (1–10). Defaults to 4.
   */
  slotCount?: number;
  [k: string]: unknown;
}
/**
 * Request body for PATCH /api/menu/{id} — all fields optional
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "UpdateMenuRequest".
 */
export interface UpdateMenuRequest {
  /**
   * New display name
   */
  name?: string;
  /**
   * New favorite flag
   */
  isFavorite?: boolean;
  /**
   * Pin (1) into the Binder or unpin (0). Flipping 0→1 cascades Pinned=1 to every meal the menu's slots reference.
   */
  pinned?: boolean;
  [k: string]: unknown;
}
/**
 * Request body for POST /api/menu/{id}/slot/{slotOrder}/meals — appends an existing meal to the slot at the next free position (0–3).
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "AddMealToSlotRequest".
 */
export interface AddMealToSlotRequest {
  /**
   * ID of the Meal to append to the slot
   */
  mealId: number;
  [k: string]: unknown;
}
/**
 * Request body for PUT /api/menu/{id}/slots/{slotOrder}/name — sets or clears the user-defined name on a slot
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "SetSlotNameRequest".
 */
export interface SetSlotNameRequest {
  /**
   * New slot name. Null explicitly clears any existing name.
   */
  slotName?: string | null;
  [k: string]: unknown;
}
/**
 * Request body for PUT /api/menu/{id}/slots/{slotOrder}/dining-out — marks a slot as dining out (or clears the flag)
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "SetDiningOutRequest".
 */
export interface SetDiningOutRequest {
  /**
   * True to mark the slot as dining out; false to clear the flag
   */
  isDiningOut: boolean;
  [k: string]: unknown;
}
/**
 * One line item on a shopping list
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "ShoppingItem".
 */
export interface ShoppingItem {
  /**
   * Food name as it should appear on the list
   */
  foodName: string;
  /**
   * Quantity scaled by rotation peopleCount and meal servings
   */
  scaledQuantity: number;
  /**
   * Unit of measurement
   */
  unit: string;
  /**
   * Optional list of meal names that contribute to this quantity
   */
  usedInMeals?: string[];
  /**
   * Optional list of menu names that contribute to this quantity
   */
  usedInMenus?: string[];
  /**
   * Optional pre-baked purchase link (e.g., Amazon URL)
   */
  productPurchaseLink?: string;
  /**
   * True when the item is a pantry staple
   */
  isStaple: boolean;
  /**
   * True when the item is an untracked freeform entry
   */
  isUntracked: boolean;
  /**
   * Optional shopper-facing note
   */
  note?: string;
  [k: string]: unknown;
}
/**
 * A named grouping of shopping items (e.g., 'Produce', 'Protein')
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "ShoppingCategory".
 */
export interface ShoppingCategory {
  /**
   * Display name of the category
   */
  categoryName: string;
  /**
   * Items in this category
   */
  items: ShoppingItem[];
  [k: string]: unknown;
}
/**
 * GET /api/rotation/{id}/shopping-list response
 *
 * This interface was referenced by `RotationSchema`'s JSON-Schema
 * via the `definition` "ShoppingList".
 */
export interface ShoppingList {
  /**
   * ID of the source rotation
   */
  rotationId: number;
  /**
   * Display name of the source rotation
   */
  rotationName: string;
  /**
   * People count the quantities were scaled to
   */
  peopleCount: number;
  /**
   * When the shopping list was generated
   */
  generatedAt: string;
  /**
   * Tracked items, grouped by category
   */
  categories: ShoppingCategory[];
  /**
   * Freeform untracked items collected from meals
   */
  untrackedItems: ShoppingItem[];
  /**
   * Pantry staple items pulled in from user prefs / curated lists
   */
  stapleItems: ShoppingItem[];
  [k: string]: unknown;
}
