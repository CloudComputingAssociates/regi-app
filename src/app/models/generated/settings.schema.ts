/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Consolidated user settings stored as JSON columns in UserSettings table
 */
export interface UserSettingsSchema {
  [k: string]: unknown;
}
/**
 * Tab persistence settings
 *
 * This interface was referenced by `UserSettingsSchema`'s JSON-Schema
 * via the `definition` "TabSettings".
 */
export interface TabSettings {
  /**
   * List of default tabs to open on login
   */
  defaultTabs: ("chat" | "menus" | "foods" | "shop" | "preferences" | "account" | "help")[];
  /**
   * The tab that had focus when settings were saved
   */
  activeTabId?: "chat" | "menus" | "foods" | "shop" | "preferences" | "account" | "help";
}
/**
 * RegiMenu meal planning settings
 *
 * This interface was referenced by `UserSettingsSchema`'s JSON-Schema
 * via the `definition` "RegiMenuSettings".
 */
export interface RegiMenuSettings {
  /**
   * Number of meals per day (1-10)
   */
  mealsPerDay?: number;
  /**
   * Intermittent fasting schedule
   */
  fastingType?: "none" | "16_8" | "18_6" | "20_4" | "omad";
  /**
   * Eating window start time in 24-hour format HH:MM
   */
  eatingStartTime?: string;
  /**
   * Number of times to repeat meal plan (1-12)
   */
  repeatMeals?: number;
  /**
   * Number of people the meal plan serves
   */
  persons?: number;
  /**
   * Day of week that starts a new meal planning week
   */
  weekStartDay?: "sunday" | "monday" | "tuesday" | "wednesday" | "thursday" | "friday" | "saturday";
}
/**
 * Daily nutrition targets (computed by PromptMe, user may override)
 *
 * This interface was referenced by `UserSettingsSchema`'s JSON-Schema
 * via the `definition` "DailyGoals".
 */
export interface DailyGoals {
  /**
   * Daily calorie target
   */
  calories: number;
  /**
   * Daily protein target in grams
   */
  protein: number;
  /**
   * Daily carbohydrate target in grams
   */
  carbs: number;
  /**
   * Daily fat target in grams
   */
  fat: number;
  /**
   * Daily fiber target in grams
   */
  fiber: number;
  /**
   * Daily sodium target in mg
   */
  sodium: number;
  /**
   * When true, user has manually edited daily goals. UX will not overwrite with calculated values until user unchecks and saves.
   */
  isOverridden?: boolean;
}
/**
 * User physical profile for nutrition calculations
 *
 * This interface was referenced by `UserSettingsSchema`'s JSON-Schema
 * via the `definition` "PersonalInfo".
 */
export interface PersonalInfo {
  /**
   * Biological sex for BMR calculation
   */
  sex?: "male" | "female";
  /**
   * Date of birth in ISO format (YYYY-MM-DD)
   */
  dateOfBirth?: string;
  /**
   * Height in centimeters
   */
  heightCm?: number;
  /**
   * Current weight in kilograms
   */
  currentWeightKg?: number;
  /**
   * Target/goal weight in kilograms
   */
  targetWeightKg?: number;
  /**
   * Physical activity level for TDEE calculation. light_moderate (1.4654 multiplier, 4-5 days/week) sits between lightly_active (1.375) and moderately_active (1.55) for finer granularity, mirroring the 6-tier scale calculator.net uses.
   */
  activityLevel?:
    | "sedentary"
    | "lightly_active"
    | "light_moderate"
    | "moderately_active"
    | "very_active"
    | "extremely_active";
  /**
   * Protein ratio in g per lb of target weight
   */
  proteinRatio?: 0.8 | 1 | 1.2;
  /**
   * Daily carb target in grams (slider value)
   */
  carbScaleGrams?: number;
  /**
   * Caloric adjustment percent: negative = deficit (losing weight), positive = surplus (gaining weight). E.g. -20 means 20% lowered, +10 means 10% raised.
   */
  deficitPercent?: number;
  /**
   * Calculated daily calorie target derived from BMR/TDEE/deficit
   */
  calcCalories?: number;
  /**
   * Calculated daily protein in grams (proteinRatio × targetWeight in lbs)
   */
  calcProtein?: number;
  /**
   * Calculated daily fat in grams (remaining calories after protein and carbs)
   */
  calcFats?: number;
  /**
   * Calculated daily carbs in grams (from carbScaleGrams)
   */
  calcCarbs?: number;
}
/**
 * A single shopping staple item
 *
 * This interface was referenced by `UserSettingsSchema`'s JSON-Schema
 * via the `definition` "ShoppingStaple".
 */
export interface ShoppingStaple {
  /**
   * Unique identifier for the staple
   */
  id: string;
  /**
   * Shopping category
   */
  category: "proteins" | "produce" | "bulk" | "dairy" | "aisles" | "non_food";
  /**
   * Item name
   */
  item: string;
  /**
   * Quantity (e.g. '2', '1 bunch')
   */
  qty?: string;
  /**
   * Store name (optional)
   */
  store?: string;
  /**
   * Whether this staple is needed for the current shopping trip
   */
  needed?: boolean;
  /**
   * Whether this item has been picked up during shopping
   */
  pickedUp?: boolean;
  /**
   * Display order within category
   */
  sortOrder?: number;
}
/**
 * A single basket pick (curated food staged for menu planning)
 *
 * This interface was referenced by `UserSettingsSchema`'s JSON-Schema
 * via the `definition` "CurrentPick".
 */
export interface CurrentPick {
  /**
   * References Foods.FoodID (foodSource='food') or UserFoods.UserFoodID (foodSource='userfood')
   */
  foodId: number;
  /**
   * Discriminator selecting which table foodId references
   */
  foodSource: "food" | "userfood";
  /**
   * Which UI basket this pick belongs to
   */
  basketKey: "Proteins" | "Fats" | "Carbs" | "Other";
  /**
   * Per-basket serving-size override; null = use the food's baseline serving
   */
  pickServingSize?: number | null;
  /**
   * How the generator should use this pick: PrimaryFood = build the meal around it; SecondaryFood = supporting/complementary; AnyUse = no constraint (default).
   */
  mealRole?: "AnyUse" | "PrimaryFood" | "SecondaryFood";
  /**
   * When the pick was added — used for stable ordering within a basket
   */
  addedAt: string;
}
/**
 * Consolidated response for GET /api/user/settings (all settings at once)
 *
 * This interface was referenced by `UserSettingsSchema`'s JSON-Schema
 * via the `definition` "AllSettingsResponse".
 */
export interface AllSettingsResponse {
  tabs?: TabSettings;
  regiMenu?: RegiMenuSettings;
  /**
   * Default food list filter
   */
  defaultFoodList?: "yeh_approved" | "all_foods";
  dailyGoals?: DailyGoals;
  personalInfo?: PersonalInfo;
  /**
   * User's recurring shopping staple items
   */
  shoppingStaples?: ShoppingStaple[];
  /**
   * User's curated basket picks (server-persisted staging for menu planning)
   *
   * @maxItems 200
   */
  currentPicks?: CurrentPick[];
}
